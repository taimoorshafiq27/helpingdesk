<%# <% if current_user&.administrator? %>
    <%#= button_to "Admin", admin_screens_path %>
<%# end %>

<div class="flex min-h-screen">
  <%= render MenuComponent.new %>
    <div class="w-full">
        <div class="flex py-6 px-24 bg-white">
            <h1 class="flex-1">Dashboard</h1>
            <div class="flex items-center ml-auto space-x-2 pr-10">
                <i class="fa-solid fa-user"></i>
                <%= link_to "My account", user_path(current_user) %>
            </div>
            <% if current_user %>
              <%= link_to "Log out", destroy_user_session_path, data: { turbo_method: :delete }, class: "px-2 py-1 rounded bg-red-500 text-white hover:bg-red-600" %>
            <% end %>
        </div>
        <div class="grid grid-cols-3 gap-4 px-24 py-6">
            <div class="h-64 bg-white">column 1</div>
            <div class="bg-white">column 2</div>
            <div class="h-64 bg-white">column 3</div>
            <div class="bg-white">column 4</div>
             <div class="bg-white">column 5</div>
            <div class="bg-white">column 6</div>
        </div>
    </div>
</div>